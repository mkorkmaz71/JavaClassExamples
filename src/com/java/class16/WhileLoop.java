package com.java.class16;

public class WhileLoop {
    public static void main(String[] args) {
        /**       int activeusers = 0;
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         activeusers++;
         System.out.println("Welcome user number " + activeusers);
         **/
        int user = 0;
        while (user < 10) {
            System.out.println("Welcome user number " + ++user);

        }
    }
}
